<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modifier {{ article.title }} - BDP Admin</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        'electric-blue': '#00A2FF',
                        'electric-blue-dark': '#0088D1',
                        'electric-blue-light': '#4DC1FF',
                        'passion-orange': '#FF6B35',
                        'passion-orange-dark': '#E05A2B',
                        'passion-orange-light': '#FF8C5A',
                        'text-primary': '#1A1A1A',
                        'text-secondary': '#666666',
                        'text-tertiary': '#999999',
                        'bg-light': '#F5F5F5',
                        'border-light': '#E5E5E5',
                    },
                    fontFamily: {
                        'heading': ['Montserrat', 'sans-serif'],
                        'body': ['Inter', 'sans-serif'],
                    }
                }
            }
        }
    </script>
</head>

<body class="bg-gray-50 min-h-screen">
    <!-- Header Admin -->
    <header class="bg-white shadow-sm border-b">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center py-4">
                <!-- Logo et titre -->
                <div class="flex items-center space-x-4">
                    <a href="/admin/dashboard" class="flex items-center space-x-2 no-underline">
                        <div
                            class="w-8 h-8 bg-blue-600 rounded-full flex items-center justify-center text-white font-bold text-sm">
                            BDP
                        </div>
                        <span class="font-bold text-gray-900">Admin BDP</span>
                    </a>
                    <span class="text-gray-400">|</span>
                    <h1 class="text-xl font-semibold text-gray-900">Modifier l'article</h1>
                </div>

                <!-- Menu utilisateur -->
                <div class="flex items-center space-x-4">
                    <span class="text-sm text-gray-600">{{ user.fullName }}</span>
                    <a href="/admin/logout"
                        class="bg-red-500 hover:bg-red-600 text-white px-4 py-2 rounded-lg text-sm font-medium transition-colors">
                        🚪 Déconnexion
                    </a>
                </div>
            </div>
        </div>
    </header>

    <!-- Navigation secondaire -->
    <div class="bg-white border-b">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex space-x-8 py-4">
                <a href="/admin/dashboard" class="text-gray-500 hover:text-gray-700 font-medium transition-colors">
                    📊 Dashboard
                </a>
                <a href="/admin/articles" class="text-blue-600 border-b-2 border-blue-600 font-medium">
                    📝 Articles
                </a>
                <a href="#" class="text-gray-500 hover:text-gray-700 font-medium transition-colors">
                    👥 Utilisateurs
                </a>
                <a href="#" class="text-gray-500 hover:text-gray-700 font-medium transition-colors">
                    ⚙️ Paramètres
                </a>
            </div>
        </div>
    </div>

    <!-- Main Content -->
    <main class="max-w-6xl mx-auto py-8 px-4 sm:px-6 lg:px-8">
        <!-- Header avec actions -->
        <div class="flex justify-between items-center mb-8">
            <div>
                <nav class="flex items-center space-x-2 text-sm text-gray-500 mb-4">
                    <a href="/admin/articles" class="hover:text-gray-700">Articles</a>
                    <span>/</span>
                    <a href="/admin/articles/{{ article.id }}" class="hover:text-gray-700">{{ article.title }}</a>
                    <span>/</span>
                    <span class="text-gray-900">Modifier</span>
                </nav>
                <h1 class="text-3xl font-bold text-gray-900">Modifier l'article</h1>
            </div>
            <div class="flex space-x-3">
                <a href="/admin/articles/{{ article.id }}"
                    class="bg-gray-500 hover:bg-gray-600 text-white font-semibold py-2 px-6 rounded-lg transition-all duration-200 flex items-center space-x-2">
                    <span>←</span>
                    <span>Retour</span>
                </a>
                <a href="/article/{{ article.slug }}" target="_blank"
                    class="bg-green-600 hover:bg-green-700 text-white font-semibold py-2 px-6 rounded-lg transition-colors flex items-center space-x-2">
                    <span>👁️</span>
                    <span>Voir sur le site</span>
                </a>
            </div>
        </div>

        <form action="/admin/articles/{{ article.id }}" method="POST" enctype="multipart/form-data">
            {{ csrfField() }}
            <input type="hidden" name="_method" value="PUT">

            <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
                <!-- Colonne principale -->
                <div class="lg:col-span-2 space-y-6">
                    <!-- Titre -->
                    <div class="bg-white rounded-lg shadow-sm p-6">
                        <label for="title" class="block text-sm font-medium text-gray-700 mb-2">
                            Titre de l'article *
                        </label>
                        <input type="text" id="title" name="title" value="{{ article.title }}" required
                            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 text-lg font-medium"
                            placeholder="Entrez le titre de l'article">
                    </div>

                    <!-- Résumé -->
                    <div class="bg-white rounded-lg shadow-sm p-6">
                        <label for="resume" class="block text-sm font-medium text-gray-700 mb-2">
                            Résumé *
                        </label>
                        <textarea id="resume" name="resume" rows="4" required
                            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 resize-none"
                            placeholder="Résumé court de l'article (affiché dans les listes)">{{ article.resume }}</textarea>
                        <p class="text-sm text-gray-500 mt-2">Ce résumé sera affiché dans les listes d'articles.</p>
                    </div>

                    <!-- Contenu Markdown -->
                    <div class="bg-white rounded-lg shadow-sm p-6">
                        <label for="content" class="block text-sm font-medium text-gray-700 mb-2">
                            Contenu de l'article (Markdown) *
                        </label>
                        <textarea id="content" name="content" rows="20"
                            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 resize-none font-mono text-sm"
                            placeholder="# Titre

## Sous-titre

**Texte en gras** et *texte en italique*

- Liste
- Élément

```javascript
// Code
console.log('Hello');
Citation">{{ article.content }}</textarea>
                        <p class="text-sm text-gray-500 mt-2">
                            Utilisez la syntaxe Markdown comme sur GitHub. Le contenu sera automatiquement formaté sur
                            le site.
                        </p>
                    </div>
                </div>
                <!-- Colonne latérale -->
                <div class="space-y-6">
                    <!-- Publication -->
                    <div class="bg-white rounded-lg shadow-sm p-6">
                        <h3 class="text-lg font-medium text-gray-900 mb-4">Publication</h3>

                        <!-- Statut -->
                        <div class="mb-4">
                            <label class="block text-sm font-medium text-gray-700 mb-2">Statut</label>
                            <select name="status"
                                class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500">
                                <option value="draft" {{ article.status==='draft' ? 'selected' : '' }}>📝 Brouillon
                                </option>
                                <option value="published" {{ article.status==='published' ? 'selected' : '' }}>✅ Publié
                                </option>
                            </select>
                        </div>

                        <!-- Catégorie -->
                        <div class="mb-4">
                            <label class="block text-sm font-medium text-gray-700 mb-2">Catégorie</label>
                            <select name="category"
                                class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500">
                                <option value="🤖 IA & ML" {{ article.category==='🤖 IA & ML' ? 'selected' : '' }}>🤖 IA
                                    & ML</option>
                                <option value="💻 Développement Web" {{ article.category==='💻 Développement Web'
                                    ? 'selected' : '' }}>
                                    💻 Développement Web</option>
                                <option value="📱 Mobile" {{ article.category==='📱 Mobile' ? 'selected' : '' }}>📱
                                    Mobile</option>
                                <option value="🛠️ Outils" {{ article.category==='🛠️ Outils' ? 'selected' : '' }}>🛠️
                                    Outils</option>
                            </select>
                        </div>

                        <!-- Auteur -->
                        <div>
                            <label for="author" class="block text-sm font-medium text-gray-700 mb-2">Auteur</label>
                            <input type="text" id="author" name="author" value="{{ article.author || '' }}"
                                class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
                                placeholder="Nom de l'auteur">
                        </div>
                    </div>

                    <!-- Image principale -->
                    <div class="bg-white rounded-lg shadow-sm p-6">
                        <h3 class="text-lg font-medium text-gray-900 mb-4">Image principale</h3>

                        @if(article.medias && article.medias.length > 0)
                        <div class="mb-4">
                            <img src="/{{ article.medias[0].filePath }}" alt="Image actuelle"
                                class="w-full h-32 object-cover rounded-lg mb-2">
                            <p class="text-sm text-gray-500">Image actuelle</p>
                        </div>
                        @endif

                        <div>
                            <label for="image" class="block text-sm font-medium text-gray-700 mb-2">
                                Nouvelle image
                            </label>
                            <input type="file" id="image" name="image" accept="image/*"
                                class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                            <p class="text-sm text-gray-500 mt-2">Formats supportés: JPG, PNG, GIF. Taille max: 5MB</p>
                        </div>
                    </div>

                    <!-- Actions -->
                    <div class="bg-white rounded-lg shadow-sm p-6">
                        <h3 class="text-lg font-medium text-gray-900 mb-4">Actions</h3>
                        <div class="space-y-3">
                            <button type="submit"
                                class="w-full bg-passion-orange hover:bg-orange-600 text-white font-semibold py-3 px-4 rounded-lg transition-all duration-200 transform hover:-translate-y-1 hover:shadow-lg flex items-center justify-center space-x-2">
                                <span>💾</span>
                                <span>Enregistrer les modifications</span>
                            </button>

                            <a href="/admin/articles/{{ article.id }}"
                                class="block w-full bg-gray-500 hover:bg-gray-600 text-white font-semibold py-3 px-4 rounded-lg transition-all duration-200 flex items-center justify-center space-x-2">
                                <span>←</span>
                                <span>Annuler</span>
                            </a>

                            <form action="/admin/articles/{{ article.id }}" method="POST" class="inline w-full">
                                {{ csrfField() }}
                                <input type="hidden" name="_method" value="DELETE">
                                <button type="submit"
                                    onclick="return confirm('Êtes-vous sûr de vouloir supprimer cet article ? Cette action est irréversible.')"
                                    class="w-full bg-red-600 hover:bg-red-700 text-white font-semibold py-3 px-4 rounded-lg transition-colors flex items-center justify-center space-x-2">
                                    <span>🗑️</span>
                                    <span>Supprimer l'article</span>
                                </button>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </form>
    </main>
</body>

</html> ```