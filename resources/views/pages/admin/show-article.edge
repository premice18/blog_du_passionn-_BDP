<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ article.title }} - BDP Admin</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        'electric-blue': '#00A2FF',
                        'electric-blue-dark': '#0088D1',
                        'electric-blue-light': '#4DC1FF',
                        'passion-orange': '#FF6B35',
                        'passion-orange-dark': '#E05A2B',
                        'passion-orange-light': '#FF8C5A',
                        'text-primary': '#1A1A1A',
                        'text-secondary': '#666666',
                        'text-tertiary': '#999999',
                        'bg-light': '#F5F5F5',
                        'border-light': '#E5E5E5',
                    },
                    fontFamily: {
                        'heading': ['Montserrat', 'sans-serif'],
                        'body': ['Inter', 'sans-serif'],
                    }
                }
            }
        }
    </script>
</head>

<body class="bg-gray-50 min-h-screen">
    <!-- Header Admin -->
    <header class="bg-white shadow-sm border-b">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center py-4">
                <!-- Logo et titre -->
                <div class="flex items-center space-x-4">
                    <a href="/admin/dashboard" class="flex items-center space-x-2 no-underline">
                        <div
                            class="w-8 h-8 bg-blue-600 rounded-full flex items-center justify-center text-white font-bold text-sm">
                            BDP
                        </div>
                        <span class="font-bold text-gray-900">Admin BDP</span>
                    </a>
                    <span class="text-gray-400">|</span>
                    <h1 class="text-xl font-semibold text-gray-900">Détail de l'article</h1>
                </div>

                <!-- Menu utilisateur -->
                <div class="flex items-center space-x-4">
                    <span class="text-sm text-gray-600">{{ user.fullName }}</span>
                    <a href="/admin/logout"
                        class="bg-red-500 hover:bg-red-600 text-white px-4 py-2 rounded-lg text-sm font-medium transition-colors">
                        🚪 Déconnexion
                    </a>
                </div>
            </div>
        </div>
    </header>

    <!-- Navigation secondaire -->
    <div class="bg-white border-b">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex space-x-8 py-4">
                <a href="/admin/dashboard" class="text-gray-500 hover:text-gray-700 font-medium transition-colors">
                    📊 Dashboard
                </a>
                <a href="/admin/articles" class="text-blue-600 border-b-2 border-blue-600 font-medium">
                    📝 Articles
                </a>
                <a href="#" class="text-gray-500 hover:text-gray-700 font-medium transition-colors">
                    👥 Utilisateurs
                </a>
                <a href="#" class="text-gray-500 hover:text-gray-700 font-medium transition-colors">
                    ⚙️ Paramètres
                </a>
            </div>
        </div>
    </div>

    <!-- Main Content -->
    <main class="max-w-4xl mx-auto py-8 px-4 sm:px-6 lg:px-8">
        <!-- Header avec actions -->
        <div class="flex justify-between items-center mb-8">
            <div>
                <nav class="flex items-center space-x-2 text-sm text-gray-500 mb-4">
                    <a href="{{ route('dashboard') }}" class="hover:text-gray-700">Articles</a>
                    <span>/</span>
                    <span class="text-gray-900">Détail</span>
                </nav>
                <h1 class="text-3xl font-bold text-gray-900">{{ article.title }}</h1>
            </div>
            <div class="flex space-x-3">
                <a href="/admin/articles/{{ article.id }}/edit"
                    class="bg-electric-blue hover:bg-electric-blue-dark text-white font-semibold py-2 px-6 rounded-lg transition-all duration-200 transform hover:-translate-y-1 hover:shadow-lg flex items-center space-x-2">
                    <span>✏️</span>
                    <span>Modifier</span>
                </a>
                <a href="{{ route('dashboard') }}"
                    class="bg-gray-500 hover:bg-gray-600 text-white font-semibold py-2 px-6 rounded-lg transition-all duration-200 flex items-center space-x-2">
                    <span>←</span>
                    <span>Retour</span>
                </a>
            </div>
        </div>

        <div class="bg-white rounded-lg shadow-sm overflow-hidden">
            <!-- En-tête de l'article -->
            <div class="border-b border-gray-200 p-6">
                <div class="flex flex-col lg:flex-row lg:items-start lg:justify-between">
                    <div class="flex-1">
                        <div class="flex items-center space-x-4 mb-4">
                            <span
                                class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-blue-100 text-blue-800">
                                {{ article.category }}
                            </span>
                            @if(article.status === 'published')
                            <span
                                class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-green-100 text-green-800">
                                ✅ Publié
                            </span>
                            @else
                            <span
                                class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-yellow-100 text-yellow-800">
                                📝 Brouillon
                            </span>
                            @endif
                            <span class="text-sm text-gray-500">
                                Créé le {{ article.createdAt.toRelative() }}
                            </span>
                        </div>

                        @if(article.medias && article.medias.length > 0)
                        <div class="mb-6">
                            <img src="/{{ article.medias[0].filePath }}" alt="{{ article.title }}"
                                class="w-full h-64 object-cover rounded-lg">
                        </div>
                        @endif
                    </div>
                </div>
            </div>

            <!-- Contenu de l'article -->
            <div class="p-6">
                <div class="prose max-w-none">
                    <h2 class="text-xl font-semibold text-gray-900 mb-4">Résumé</h2>
                    <p class="text-gray-700 mb-8 leading-relaxed">{{ article.resume }}</p>

                    <h2 class="text-xl font-semibold text-gray-900 mb-4">Contenu</h2>
                    <div class="text-gray-700 leading-relaxed prose prose-sm max-w-none bg-gray-50 p-6 rounded-lg">
                        {{{ markdown(article.content) }}}
                    </div>
                </div>

                <!-- Métadonnées -->
                <div class="mt-8 pt-6 border-t border-gray-200">
                    <h3 class="text-lg font-semibold text-gray-900 mb-4">Informations</h3>
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4 text-sm">
                        <div>
                            <span class="font-medium text-gray-500">Auteur:</span>
                            <span class="ml-2 text-gray-900">{{ article.user.fullName }}</span>
                        </div>
                        <div>
                            <span class="font-medium text-gray-500">Date de création:</span>
                            <span class="ml-2 text-gray-900">{{ article.createdAt.toRelative() }}</span>
                        </div>
                        <div>
                            <span class="font-medium text-gray-500">Dernière modification:</span>
                            <span class="ml-2 text-gray-900">{{ article.updatedAt.toRelative() }}</span>
                        </div>
                        <div>
                            <span class="font-medium text-gray-500">Nombre de vues:</span>
                            <span class="ml-2 text-gray-900">{{ article.views || 0 }}</span>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Actions -->
            <div class="bg-gray-50 px-6 py-4 border-t border-gray-200">
                <div class="flex justify-between items-center">
                    <div class="text-sm text-gray-500">
                        ID: {{ article.id }}
                    </div>
                    <div class="flex space-x-3">
                        <a href="{{ route('article.show', { id: article.id }) }}" target="_blank"
                            class="bg-green-600 hover:bg-green-700 text-white font-medium py-2 px-4 rounded-lg transition-colors flex items-center space-x-2">
                            <span>👁️</span>
                            <span>Voir sur le site</span>
                        </a>
                        <form action="{{ route('deleteArticle', { id: article.id }) }}" method="POST" class="inline">
                            {{ csrfField() }}
                            <button type="submit"
                                onclick="return confirm('Êtes-vous sûr de vouloir supprimer cet article ? Cette action est irréversible.')"
                                class="bg-red-600 hover:bg-red-700 text-white font-medium py-2 px-4 rounded-lg transition-colors flex items-center space-x-2">
                                <span>🗑️</span>
                                <span>Supprimer</span>
                            </button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </main>
</body>

</html>